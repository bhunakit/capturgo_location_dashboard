export interface LocationData {
  latitude: number;
  longitude: number;
  created_at: string;
  speed: number;
  user_id: string;
}

export interface UserIdOption {
  value: string;
  label: string;
}
